"""geoserve_titiler — local extension layered onto titiler-pgstac.

Lives in /opt/geoserve_titiler/ inside the container (see Dockerfile).
Importable as `geoserve_titiler.*` because PYTHONPATH is set there.
"""
